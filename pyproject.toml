[project]
name = "crosstalk"
version = "0.1.0"
description = "LLM駆動の恋愛ドロドロ群像劇シミュレーションゲーム"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "litellm>=1.40",
    "pydantic>=2.6",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
